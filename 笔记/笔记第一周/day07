目录：
一、布尔类型
二、字符串



总结：
1.布尔型
    1-：布尔类型分为两种，true 和 false，默认是false。
    2-：布尔类型不能与整型之间进行转换，也不能和其他类型进行转换

2.字符串:定义，转义，多行字符串
    1- 定义字符串的三种方式
    2-字符串的转义，有几种常见的：换行\n ，\\表示输出\，用于输出目录
        \',表示输出单引号，\"表示输出双引号
    3- 输出多行字符串，使用 ``
3.字符串常见的操作
    1-获取字符串的长度 len（）
    2-拼接字符串，使用 + 或者 Sprintf来拼接。
    3-分割字符串。strings.Split()
    4-切片连接成字符串，strings.Join()
    5-判断一个字符串里面是否包含另外一个字符串，strings.Contains(1，2),判断1是否包含2，返回一个bool值
    6-判断字符串的前缀和后缀,strings.HasPrefix(),,strings.HasSuffix()
    7-判断子字符串出现的位置，strings.Index(),strings.LastIndex()。查找到返回对应的下标，查找不到返回-1