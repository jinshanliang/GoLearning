//2026-4-5 学习笔记
//1.switch case 的用法
2.循环跳出语句 break continue goto

3. 数组





笔记：
1.switch case 是针对大量的值进行判断
   1.1
    switch 判断的内容{
    case 
        break
    }
    1.2 每一个switch case 中只能有一个default
    1.3 还有另外一种写法，switch extreme：=“名字”；extreme{case}
    1.4 可以不用写break
    1.5 一个分支可以有多个值，多个值之间是需要使用英文的逗号隔开
    1.6 分支case后面还可以使用表达式，这个时候switch后面不需要再跟判断变量
    1.7 switcg case 中有一个 fallthrough 是可以穿透下一个case的，比如在这个case中
        使用了 fallthrough 以后会继续执行下一个case。注意的是一次只能穿透一层。
2.循环跳出语句 break 和 continue
    2.1 break 表示跳出当前循环
    2.2 可以使用lable 进行跳出多层循环。lable名字是可以任意写的，可以是任意的名字   
        例如
        lable:
            for i := 1; i < 2; i++ {
                for j := 0; j < 10; j++ {
                    break lable
                }
            }
        2.3 continue 是结束本次循环，继续下一次循环。是不需要单独跳转到某一个循环的，可以单独使用continue

3. 跳转到指定的标签，goto（进行代码间的无条件跳转）


4.数组：数组是同一数据类型的集合，并且长度也是类型的一部分
4.1数组的声明
    var 数组名称 [长度]类型
4.2 数组的初始化
    第一种：根据数组的下标进行赋值。