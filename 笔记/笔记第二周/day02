

目录：
1.数值类型之间的相互转换
2.整型和整型之间的相互转换，浮点型和浮点型之间的相互转换
3.整型和浮点型之间的转换。
    注意：1.将整型转换成浮点型，而不是将浮点型转换成整型，容易出现数据丢失
         2.通过将低位转换成高位，高位转低位容易出现数据溢出，造成丢失
4.其他类型转换成string字符串
    注：1.通过Sprintf 进行转换。 里面的格式：int 是%d ，float 是%f  bool是%t
5.通过 strconv 把其他类型转换成字符串
    5.1 FormatInt 两个参数，第一个参数必须是int64（），进制数
    5.2 FormatFloat 
6.string 转换成整型int
    使用strconv.ParseInt() 的方法，返回的是两个值
7.string 转换成float
    使用strconv.ParseFloat() 的方法，返回值是两个，不懂了可以直接点开方法的具体参数