


目录：
1.算数运算符 加减乘除取余
    注意：1.除法的时候，如果都是整型，那么结果也是整型，如果是浮点型，那么结果也是浮点型
2.自增自减 ，在go中不是算数运算符。不允许赋值，只能单独使用；只有后++ ，没有前++
3.关系运算符：==，！=，>,>=,<,<=.    返回的类型是布尔值
4.逻辑运算符 ，&&，||,！
    逻辑与和逻辑或 有短路
5.赋值运算符。
    =，+=，-=，*=，/=，%=
