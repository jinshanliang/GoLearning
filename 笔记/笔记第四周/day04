2026-4-9 学习笔记
目录：
1.切片




1.什么是切片：切片是相同类型长度可变的序列
2.切片的定义模版： var sli = []int{1,2,3,4,5}
3.切片定义： var sli = []int{1:1,2:3,3:6} 前面是对应的下标后面是值，跳过的默认值为0
4.声明以后切片的默认值的nil
5.基于数组定义切片
	5.1 
		var a = [5]{1,2,3,4,5}
		b := a[:].  //b的值就是 {1,2,3,4,5}，但是它的类型就是切片
		c := a[1:4] //实现的是左包右不包
		d := a[2:] //获取到  3，4，5，
6.基于切片定义切片，和基于数组定义切片是一样的。
7.切片拥有自己的长度和容量。使用len()获取长度，使用 cap() 来获取它的容量
	 切片的长度：就是切片元素的个数
	 切片的容量：切片从它第一个元素其底层数组元素的个数
8.使用make（）函数来构造切片。  make(T,len,cap) 类型，长度，容量
	8.1 创建切片
		var a = make([]int, 3, 3)
		fmt.Println(a)  //[0 0 0]
	8.2 修改切片：直接通过下标的方式进行修改

	8.3 扩容，无法通过下标的方式进行扩容，可以通过使用append（）方法。
		append（需要加的切片，需要加的值）
		
